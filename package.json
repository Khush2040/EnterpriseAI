{
  "name": "enterprise-ai",
  "version": "1.0.0",
  "description": "Enterprise AI full stack deployment",
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
